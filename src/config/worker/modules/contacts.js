export default {
  tasks: {
    // check({api, store}, list) {
    //   return api.res(`mailer/check_${list}`, 'raw').load().then(() => {
    //     store.commit('contacts/status', 8);
    //     store.commit('contacts/notifi', false);
    //   });
    // },
  },
};
